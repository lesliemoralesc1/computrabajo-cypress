{
  "name": "computrabajo-cypress",
  "version": "1.0.0",
  "description": "Automatización de pruebas para Computrabajo con Cypress",
  "main": "index.js",
    "scripts": {
      "test": "cypress run && npm run report:allure && npm run report:open",
      "test:headed": "cypress run --headed && npm run report:allure && npm run report:open",
      "test:chrome": "cypress run --browser chrome && npm run report:allure && npm run report:open",
      "test:headed:chrome": "cypress run --headed --browser chrome && npm run report:allure && npm run report:open",
      "test:debug": "cypress run --inspect && npm run report:allure && npm run report:open",
      "test:allure": "cypress run && npm run report:allure && npm run report:open",
      "test:ci": "cypress run && npm run report:allure",
      "report:allure": "allure generate allure-results --clean -o allure-report",
      "report:open": "allure open allure-report",
      "open": "cypress open"
    },
  "keywords": [],
  "author": "Leslie Liseth Morales Casseres",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.32.1",
    "cypress": "^15.7.1"
  }
}
